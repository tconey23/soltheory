import { useEffect, useState } from 'react';
import { Button, Stack, Typography, List, ListItem, Box } from '@mui/material';

const Pic6 = () => {
  return (
    <Stack direction={'column'} border={'1px solid black'} sx={{ height: '70vh', width: '75%'}}>
        
    </Stack>
  );
};

export default Pic6;